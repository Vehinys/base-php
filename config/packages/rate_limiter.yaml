framework:
    rate_limiter:
        registration:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
        password_reset_request:
            policy: 'sliding_window'
            limit: 3
            interval: '1 hour'
