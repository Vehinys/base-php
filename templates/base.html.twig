{#
    Layout de base hérité par tous les templates de l'application.

    Blocs disponibles pour les templates enfants :
      - block title       : balise <title> (défaut : app.name)
      - block description : meta description (défaut : app.description)
      - block robots      : meta robots (défaut : index,follow — surcharger avec noindex,nofollow sur pages privées)
      - block og_title    : titre Open Graph (défaut : block title)
      - block og_description : description Open Graph (défaut : block description)
      - block og_image    : image Open Graph (défaut : favicon.svg)
      - block nav_items   : liens de navigation supplémentaires dans la barre desktop
      - block stylesheets : feuilles de style (Webpack Encore par défaut)
      - block javascripts : scripts JS (Webpack Encore par défaut)
      - block body        : contenu principal de la page

    Polices Google Fonts :
      Chargées via JS uniquement après consentement aux cookies (localStorage 'cookie_consent' === 'all').
      Ce pattern évite de déposer des traceurs tiers sans consentement (CNIL RGPD).
      Minifié en ligne avec nonce CSP car la CSP bloque les scripts sans attribut nonce.

    Canonical URL :
      Construite sans query string (pathInfo seul) pour éviter le duplicate content
      sur les pages dont l'URL varie selon les paramètres de filtrage ou de pagination.

    Bannière cookies :
      #cookieBanner est masqué par CSS par défaut et affiché par app.js si aucun
      consentement n'est stocké dans localStorage. L'attribut aria-live="polite"
      annonce le bandeau aux lecteurs d'écran sans interrompre la navigation.
#}
<!DOCTYPE html>
<html lang="{{ app.request.locale|slice(0,2) }}" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ 'app.name'|trans }}{% endblock %}</title>
    <meta name="description" content="{% block description %}{{ 'app.description'|trans }}{% endblock %}">
    <meta name="robots" content="{% block robots %}index,follow{% endblock %}">
    <link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml">

    {# Polices chargées via JS uniquement après consentement — conformité RGPD CNIL #}
    {# Script minifié en ligne avec nonce CSP (csp_nonce() fourni par NelmioSecurityBundle) #}
    <script nonce="{{ csp_nonce('script') }}">
    (function(){var c=localStorage.getItem('cookie_consent');if(c==='all'){var l=document.createElement('link');l.rel='stylesheet';l.href='https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400;12..96,600;12..96,700;12..96,800&family=DM+Sans:wght@400;500;600&display=swap';document.head.appendChild(l);}})();
    </script>

    {# Open Graph — partage sur réseaux sociaux. og:image est recommandé min. 1200×630px en prod. #}
    <meta property="og:title" content="{% block og_title %}{{ 'app.name'|trans }}{% endblock %}">
    <meta property="og:description" content="{% block og_description %}{{ 'app.description'|trans }}{% endblock %}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ app.request.schemeAndHttpHost ~ app.request.pathInfo }}">
    <meta property="og:image" content="{% block og_image %}{{ absolute_url(asset('favicon.svg')) }}{% endblock %}">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="{{ block('og_title') }}">
    <meta name="twitter:description" content="{{ block('og_description') }}">
    {# canonical sans query string — évite le duplicate content pour les URLs paginées #}
    <link rel="canonical" href="{{ app.request.schemeAndHttpHost ~ app.request.pathInfo }}">

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

</head>
<body class="min-h-screen flex flex-col" style="background-color:var(--color-bg);color:var(--color-text)">

    {#
        Skip link — accessibilité (RGAA 4.1 critère 12.7).
        sr-only le masque visuellement ; focus:not-sr-only le réaffiche au focus clavier.
        Obligatoire sur toute page avec une navigation répétée en haut.
    #}
    <a href="#main-content"
       class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 btn-primary"
       style="font-size:0.875rem">
        {{ 'nav.skip_to_content'|trans }}
    </a>

    {# ═══════════════════════════════════════════════════════ HEADER #}
    {# role="banner" = landmark ARIA pour les lecteurs d'écran (RGAA 12.6) #}
    <header role="banner"
            style="background:var(--color-bg);border-bottom:1px solid var(--color-border);position:sticky;top:0;z-index:40">
        <nav role="navigation"
             aria-label="{{ 'nav.main'|trans }}"
             style="height:64px;display:flex;align-items:center;justify-content:space-between;gap:1rem;padding:0 1.25rem;max-width:72rem;margin-left:auto;margin-right:auto">

            {# Logo #}
            <a href="{{ path('app_home') }}"
               aria-label="{{ 'app.name'|trans }} — {{ 'nav.home'|trans }}"
               style="font-family:var(--font-display);font-size:1.1875rem;font-weight:800;color:var(--color-text);text-decoration:none;flex-shrink:0;display:flex;align-items:center;gap:0.5rem;letter-spacing:-0.02em">
                <span aria-hidden="true" style="display:inline-block;width:10px;height:10px;background:var(--color-primary);border-radius:1px;flex-shrink:0"></span>
                {{ 'app.name'|trans }}
            </a>

            {# Nav desktop — masquée mobile, visible tablette+ (CSS .nav-desktop) #}
            <div class="nav-desktop" style="align-items:center;gap:1.5rem;flex:1">
                <a href="{{ path('app_home') }}"
                   style="font-size:0.9375rem;font-weight:500;color:{% if app.request.attributes.get('_route') == 'app_home' %}var(--color-text){% else %}var(--color-muted){% endif %};text-decoration:none;transition:color 0.15s"
                   {% if app.request.attributes.get('_route') == 'app_home' %}aria-current="page"{% endif %}>
                    {{ 'nav.home'|trans }}
                </a>
                {% block nav_items %}{% endblock %}
            </div>

            {# Actions desktop — masquées mobile, visibles tablette+ (CSS .nav-desktop) #}
            <div class="nav-desktop" style="align-items:center;gap:1rem">

                {#
                    Sélecteur de langue — aria-pressed indique la langue active aux lecteurs d'écran.
                    hreflang permet aux moteurs de recherche de découvrir l'URL alternante.
                #}
                <div role="group" aria-label="{{ 'nav.language'|trans }}"
                     style="display:flex;align-items:center;gap:2px;background:var(--color-surface-2);border:1px solid var(--color-border);border-radius:0.375rem;padding:2px">
                    <a href="{{ path('app_locale', {locale: 'fr'}) }}"
                       hreflang="fr"
                       aria-label="Français"
                       {% if app.request.locale starts with 'fr' %}aria-pressed="true"{% endif %}
                       style="padding:0.25rem 0.5rem;border-radius:0.25rem;font-size:0.75rem;font-weight:600;text-decoration:none;transition:all 0.15s;
                              {% if app.request.locale starts with 'fr' %}background:var(--color-primary);color:#fff{% else %}color:var(--color-muted){% endif %}">
                        FR
                    </a>
                    <a href="{{ path('app_locale', {locale: 'en'}) }}"
                       hreflang="en"
                       aria-label="English"
                       {% if app.request.locale starts with 'en' %}aria-pressed="true"{% endif %}
                       style="padding:0.25rem 0.5rem;border-radius:0.25rem;font-size:0.75rem;font-weight:600;text-decoration:none;transition:all 0.15s;
                              {% if app.request.locale starts with 'en' %}background:var(--color-primary);color:#fff{% else %}color:var(--color-muted){% endif %}">
                        EN
                    </a>
                </div>

                {# Auth — dropdown si connecté, liens login/register sinon #}
                {% if app.user %}
                    <div class="user-dropdown" id="userDropdown">
                        {#
                            aria-haspopup + aria-expanded gérés par app.js (listener sur #userDropdownBtn).
                            Le bouton n'a pas d'onclick inline (CSP interdit les event handlers HTML).
                        #}
                        <button id="userDropdownBtn"
                                aria-haspopup="true"
                                aria-expanded="false"
                                aria-label="{{ 'nav.user_menu'|trans }}"
                                style="display:flex;align-items:center;gap:0.5rem;background:var(--color-surface-2);border:1px solid var(--color-border);border-radius:0.5rem;padding:0.375rem 0.75rem;cursor:pointer;color:var(--color-text);font-size:0.875rem;font-weight:500">
                            {% if app.user.avatarUrl %}
                                <img src="{{ app.user.avatarUrl }}"
                                     alt="{{ app.user.name ?? app.user.email }}"
                                     width="24" height="24"
                                     loading="lazy"
                                     decoding="async"
                                     style="border-radius:50%;object-fit:cover">
                            {% else %}
                                {# Fallback initiale quand l'utilisateur n'a pas d'avatar (OAuth ou local) #}
                                <span aria-hidden="true"
                                      style="width:24px;height:24px;border-radius:50%;background:var(--color-primary);display:flex;align-items:center;justify-content:center;font-size:0.75rem;font-weight:700;color:#fff">
                                    {{ (app.user.name ?? app.user.email)|slice(0,1)|upper }}
                                </span>
                            {% endif %}
                            <span style="max-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">
                                {{ app.user.name ?? app.user.email }}
                            </span>
                            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                                <path d="M2 4l4 4 4-4"/>
                            </svg>
                        </button>
                        <div class="user-dropdown-panel" role="menu">
                            {# POST obligatoire pour le logout — CSRF requis via security.yaml (enable_csrf: true) #}
                            <form method="post" action="{{ path('app_logout') }}">
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                                <button type="submit" class="dropdown-item" role="menuitem">
                                    {{ 'nav.logout'|trans }}
                                </button>
                            </form>
                        </div>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}"
                       class="link-muted"
                       style="font-size:0.875rem;font-weight:500">
                        {{ 'nav.login'|trans }}
                    </a>
                    <a href="{{ path('app_register') }}" class="btn-primary" style="font-size:0.875rem">
                        {{ 'nav.register'|trans }}
                    </a>
                {% endif %}
            </div>

            {# Hamburger — visible mobile, masqué tablette+ (CSS .nav-hamburger) #}
            {# data-label-* permet à app.js de changer l'aria-label selon l'état ouvert/fermé #}
            <button class="nav-hamburger"
                    id="mobileMenuBtn"
                    aria-controls="mobileMenu"
                    aria-expanded="false"
                    aria-label="{{ 'nav.open_menu'|trans }}"
                    data-label-open="{{ 'nav.open_menu'|trans }}"
                    data-label-close="{{ 'nav.close_menu'|trans }}">
                <span style="display:block;width:22px;height:2px;background:var(--color-text);border-radius:2px;transition:transform 0.2s"></span>
                <span style="display:block;width:22px;height:2px;background:var(--color-text);border-radius:2px;transition:opacity 0.2s"></span>
                <span style="display:block;width:22px;height:2px;background:var(--color-text);border-radius:2px;transition:transform 0.2s"></span>
            </button>
        </nav>

        {# Menu mobile — visible uniquement quand la classe .open est ajoutée par JS #}
        <div id="mobileMenu"
             class="nav-mobile-menu"
             style="flex-direction:column;gap:0;background:var(--color-surface-2);border-top:1px solid var(--color-border);padding:1rem 1.25rem 1.5rem">
            <a href="{{ path('app_home') }}"
               style="padding:0.625rem 0;font-size:0.9375rem;font-weight:500;color:var(--color-muted);text-decoration:none;border-bottom:1px solid var(--color-border)">
                {{ 'nav.home'|trans }}
            </a>
            {% if app.user %}
                <span style="padding:0.625rem 0;font-size:0.875rem;color:var(--color-muted);border-bottom:1px solid var(--color-border)">
                    {{ app.user.name ?? app.user.email }}
                </span>
                {# Même pattern CSRF que le logout desktop #}
                <form method="post" action="{{ path('app_logout') }}">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                    <button type="submit"
                            style="padding:0.625rem 0;font-size:0.875rem;color:var(--color-muted);background:none;border:none;cursor:pointer;width:100%;text-align:left">
                        {{ 'nav.logout'|trans }}
                    </button>
                </form>
            {% else %}
                <a href="{{ path('app_login') }}"
                   style="padding:0.625rem 0;font-size:0.9375rem;color:var(--color-muted);text-decoration:none;border-bottom:1px solid var(--color-border)">
                    {{ 'nav.login'|trans }}
                </a>
                <a href="{{ path('app_register') }}"
                   style="padding:0.625rem 0;font-size:0.9375rem;font-weight:600;color:var(--color-primary);text-decoration:none">
                    {{ 'nav.register'|trans }}
                </a>
            {% endif %}
            <div style="display:flex;gap:0.5rem;padding-top:1rem">
                <a href="{{ path('app_locale', {locale: 'fr'}) }}" hreflang="fr"
                   style="font-size:0.8125rem;font-weight:600;padding:0.25rem 0.625rem;border-radius:0.25rem;text-decoration:none;
                          {% if app.request.locale starts with 'fr' %}background:var(--color-primary);color:#fff{% else %}color:var(--color-muted);background:var(--color-surface-2){% endif %}">
                    Français
                </a>
                <a href="{{ path('app_locale', {locale: 'en'}) }}" hreflang="en"
                   style="font-size:0.8125rem;font-weight:600;padding:0.25rem 0.625rem;border-radius:0.25rem;text-decoration:none;
                          {% if app.request.locale starts with 'en' %}background:var(--color-primary);color:#fff{% else %}color:var(--color-muted);background:var(--color-surface-2){% endif %}">
                    English
                </a>
            </div>
        </div>
    </header>

    {# ═══════════════════════════════════════════════════════ CONTENU #}
    {# id="main-content" est la cible du skip link — landmark ARIA main (RGAA 12.6) #}
    <main id="main-content" role="main" class="flex-1">
        {% block body %}{% endblock %}
    </main>

    {# ═══════════════════════════════════════════════════════ FOOTER #}
    {# role="contentinfo" = landmark ARIA footer (RGAA 12.6) #}
    <footer role="contentinfo"
            style="background-color:var(--color-surface);border-top:1px solid var(--color-border);margin-top:auto">

        {# Grille principale — 1 col mobile, 2 cols tablette, 4 cols desktop (CSS .footer-main) #}
        <div class="footer-main">

            {# Colonne 1 — Marque #}
            <div>
                <a href="{{ path('app_home') }}"
                   style="font-family:var(--font-display);font-size:1.125rem;font-weight:800;color:var(--color-text);text-decoration:none;display:inline-flex;align-items:center;gap:0.5rem;margin-bottom:0.75rem;letter-spacing:-0.02em">
                    <span aria-hidden="true" style="display:inline-block;width:9px;height:9px;background:var(--color-primary);border-radius:1px;flex-shrink:0"></span>
                    {{ 'app.name'|trans }}
                </a>
                <p style="font-size:0.875rem;color:var(--color-muted);line-height:1.6;max-width:260px;margin-top:0.5rem">
                    {{ 'footer.tagline'|trans }}
                </p>
                <div style="display:flex;gap:0.75rem;margin-top:1.25rem">
                    <a href="https://github.com/Vehinys/base-php"
                       target="_blank"
                       rel="noopener noreferrer"
                       aria-label="GitHub"
                       style="display:flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:0.5rem;border:1px solid var(--color-border);color:var(--color-muted);transition:color 0.15s,border-color 0.15s;text-decoration:none">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                            <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0 1 12 6.844a9.59 9.59 0 0 1 2.504.337c1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.02 10.02 0 0 0 22 12.017C22 6.484 17.522 2 12 2z"/>
                        </svg>
                    </a>
                </div>
            </div>

            {# Colonne 2 — Navigation #}
            <nav aria-label="{{ 'footer.nav_title'|trans }}">
                <p style="font-family:var(--font-display);font-size:0.8125rem;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:var(--color-text);margin-bottom:1rem" aria-hidden="true">
                    {{ 'footer.nav_title'|trans }}
                </p>
                <ul style="list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:0.625rem">
                    <li>
                        <a href="{{ path('app_home') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'nav.home'|trans }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_login') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'nav.login'|trans }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_register') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'nav.register'|trans }}
                        </a>
                    </li>
                </ul>
            </nav>

            {# Colonne 3 — Légal #}
            <nav aria-label="{{ 'footer.legal_title'|trans }}">
                <p style="font-family:var(--font-display);font-size:0.8125rem;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:var(--color-text);margin-bottom:1rem" aria-hidden="true">
                    {{ 'footer.legal_title'|trans }}
                </p>
                <ul style="list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:0.625rem">
                    <li>
                        <a href="{{ path('legal_mentions') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'footer.legal_mentions'|trans }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('legal_privacy') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'footer.legal_privacy'|trans }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('legal_cgu') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'footer.legal_cgu'|trans }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('legal_cookies') }}"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s">
                            {{ 'footer.legal_cookies'|trans }}
                        </a>
                    </li>
                </ul>
            </nav>

            {# Colonne 4 — Contact #}
            <div>
                <p style="font-family:var(--font-display);font-size:0.8125rem;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:var(--color-text);margin-bottom:1rem">
                    {{ 'footer.contact_title'|trans }}
                </p>
                <ul style="list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:0.625rem">
                    <li>
                        <a href="mailto:contact@baseapp.dev"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s;display:flex;align-items:center;gap:0.375rem">
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                                <polyline points="22,6 12,13 2,6"/>
                            </svg>
                            contact@baseapp.dev
                        </a>
                    </li>
                    <li>
                        <a href="https://github.com/Vehinys/base-php"
                           target="_blank"
                           rel="noopener noreferrer"
                           style="font-size:0.875rem;color:var(--color-muted);text-decoration:none;transition:color 0.15s;display:flex;align-items:center;gap:0.375rem">
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                                <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0 1 12 6.844a9.59 9.59 0 0 1 2.504.337c1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.02 10.02 0 0 0 22 12.017C22 6.484 17.522 2 12 2z"/>
                            </svg>
                            GitHub
                        </a>
                    </li>
                </ul>
            </div>
        </div>

        {# Barre de bas de page — colonne mobile, ligne tablette+ (CSS .footer-bottom-inner) #}
        <div style="border-top:1px solid var(--color-border)">
            <div class="footer-bottom-inner">
                <p style="font-size:0.8125rem;color:var(--color-muted);margin:0">
                    &copy; {{ "now"|date("Y") }} <strong style="color:var(--color-text)">{{ 'app.name'|trans }}</strong>.
                    {{ 'footer.all_rights'|trans }}
                </p>
                <p style="font-size:0.8125rem;color:var(--color-muted);margin:0">
                    {{ 'footer.built_with'|trans }}
                </p>
            </div>
        </div>
    </footer>

    {#
        ═══════════════════════════════════════════════════════ BANNIÈRE COOKIES
        Masquée par défaut (CSS : #cookieBanner { display: none }).
        app.js l'affiche si localStorage 'cookie_consent' n'est pas défini.
        role="dialog" + aria-live="polite" : annoncée aux lecteurs d'écran sans interruption.
        Les boutons déclenchent des callbacks JS qui mettent à jour localStorage et masquent le bandeau.
    #}
    <div id="cookieBanner" role="dialog" aria-label="{{ 'cookie.banner_text'|trans }}" aria-live="polite">
        <p>
            {{ 'cookie.banner_text'|trans }}
            <a href="{{ path('legal_cookies') }}" class="link-muted" style="margin-left:0.375rem;text-decoration:underline">
                {{ 'cookie.learn_more'|trans }}
            </a>
        </p>
        <div class="cookie-actions">
            <button id="cookieAcceptAll" class="btn-primary" style="font-size:0.8125rem;padding:0.5rem 1rem">
                {{ 'cookie.accept_all'|trans }}
            </button>
            <button id="cookieEssential" class="btn-ghost" style="font-size:0.8125rem;padding:0.5rem 1rem">
                {{ 'cookie.essential_only'|trans }}
            </button>
        </div>
    </div>

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</body>
</html>
